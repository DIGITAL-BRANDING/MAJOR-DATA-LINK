diff --git a/nixpacks.toml b/nixpacks.toml
index 7b555ef..38b64cd 100644
--- a/nixpacks.toml
+++ b/nixpacks.toml
@@ -5,7 +5,7 @@ nixPkgs = ["nodejs_22"]
 [phases.install]
 cmds = [
   "cd major_data_link_backend && NPM_CONFIG_PRODUCTION=false npm ci --cache /tmp/.npm --no-audit --no-fund",
-  "cd web && npm ci --cache /tmp/.npm --no-audit --no-fund"
+  "cd web && NPM_CONFIG_PRODUCTION=false npm ci --cache /tmp/.npm --no-audit --no-fund"
 ]
 
 [phases.build]
diff --git a/railway.json b/railway.json
index d949cec..0813dc0 100644
--- a/railway.json
+++ b/railway.json
@@ -2,7 +2,7 @@
   "$schema": "https://railway.app/railway.schema.json",
   "build": {
     "builder": "NIXPACKS",
-    "buildCommand": "cd major_data_link_backend && NPM_CONFIG_PRODUCTION=false npm install --cache /tmp/.npm --no-audit --no-fund && npm run prisma:generate && npm run build && cd ../web && npm install --cache /tmp/.npm --no-audit --no-fund && npm run build && mkdir -p ../major_data_link_backend/public/app && cp -r dist/. ../major_data_link_backend/public/app/"
+    "buildCommand": "cd major_data_link_backend && NPM_CONFIG_PRODUCTION=false npm install --cache /tmp/.npm --no-audit --no-fund && npm run prisma:generate && npm run build && cd ../web && NPM_CONFIG_PRODUCTION=false npm install --cache /tmp/.npm --no-audit --no-fund && npm run build && mkdir -p ../major_data_link_backend/public/app && cp -r dist/. ../major_data_link_backend/public/app/"
   },
   "deploy": {
     "startCommand": "cd major_data_link_backend && npm run start:prod",
