diff --git a/major_data_link_backend/src/config/env.ts b/major_data_link_backend/src/config/env.ts
index 69263a6..74e6ed1 100644
--- a/major_data_link_backend/src/config/env.ts
+++ b/major_data_link_backend/src/config/env.ts
@@ -12,14 +12,24 @@ const EnvSchema = z.object({
   ALRAHUZ_API_TOKEN: z.string().optional(),
 
   // BilalSadaSub - second data/airtime/cable/electricity/result-pin
-  // provider (see bilalsadasub.service.ts). Auth is username+password (not
-  // a static token like Alrahuz) - the service exchanges these for an
-  // AccessToken at runtime and caches it in memory, re-generating on a 401.
-  // Both optional so a deploy with no BilalSadaSub account configured yet
-  // doesn't fail startup - PricingSettings.dataAirtimeProvider/
+  // provider (see bilalsadasub.service.ts). Two ways to authenticate - set
+  // EITHER, not necessarily both:
+  //   1. BILALSADASUB_API_TOKEN - a token you already generated (e.g. from
+  //      the BilalSadaSub dashboard, or by POSTing to /api/user yourself
+  //      once) - used exactly as-is, no auto-refresh possible since we
+  //      never have the credentials to regenerate it if it's revoked.
+  //   2. BILALSADASUB_USERNAME + BILALSADASUB_PASSWORD - the service
+  //      exchanges these for an AccessToken at runtime, caches it in
+  //      memory, and transparently regenerates it on a 401 (token
+  //      expired/rotated). More resilient than a static token, since it
+  //      self-heals if the token changes on BilalSadaSub's end.
+  // If both are set, the static token wins (skips the auth call entirely).
+  // All three optional so a deploy with no BilalSadaSub account configured
+  // yet doesn't fail startup - PricingSettings.dataAirtimeProvider/
   // resultPinProvider just can't be switched to 'bilalsadasub' (and cable/
-  // electricity purchases will fail with a clear error) until they're set.
+  // electricity purchases will fail with a clear error) until one path is set.
   BILALSADASUB_BASE_URL: z.string().url().default('https://bilalsadasub.com'),
+  BILALSADASUB_API_TOKEN: z.string().optional(),
   BILALSADASUB_USERNAME: z.string().optional(),
   BILALSADASUB_PASSWORD: z.string().optional(),
   ALRAHUZ_DATA_PLANS_PATH: z.string().default('/data/'),
diff --git a/major_data_link_backend/src/services/bilalsadasub.service.ts b/major_data_link_backend/src/services/bilalsadasub.service.ts
index d48bd2f..a102891 100644
--- a/major_data_link_backend/src/services/bilalsadasub.service.ts
+++ b/major_data_link_backend/src/services/bilalsadasub.service.ts
@@ -61,23 +61,25 @@ type BilalResponse = {
   [key: string]: unknown;
 };
 
-// ---- Auth: username+password -> cached AccessToken ----
-// Unlike Alrahuz's ALRAHUZ_API_TOKEN (a static value pasted into env vars),
-// BilalSadaSub's docs describe generating a token from credentials once and
-// reusing it - it reads like a long-lived reseller API key rather than a
-// short-lived JWT, so this caches it in memory indefinitely and only
-// re-generates on an explicit 401 from a real API call, rather than on a
-// timer. That cache is per-process (lost on every Railway restart/deploy) -
-// acceptable since generating a fresh one is a single cheap call.
-let cachedToken: string | null = null;
+// ---- Auth ----
+// Two paths, see env.ts's doc comment on BILALSADASUB_API_TOKEN for the
+// tradeoffs. A static token is used exactly as-is - if it's ever revoked,
+// every call fails with a clear "check the token" log rather than trying
+// (and failing, since there are no credentials to regenerate from) to
+// silently auto-refresh forever.
+let cachedGeneratedToken: string | null = null;
 let pendingTokenRequest: Promise<string> | null = null;
 
-function credentialsConfigured() {
+function hasStaticToken() {
+  return Boolean(env.BILALSADASUB_API_TOKEN);
+}
+
+function hasCredentials() {
   return Boolean(env.BILALSADASUB_USERNAME && env.BILALSADASUB_PASSWORD);
 }
 
 async function generateToken(): Promise<string> {
-  if (!credentialsConfigured()) {
+  if (!hasCredentials()) {
     throw new ApiError(500, 'BilalSadaSub is not configured', 'BILALSADASUB_NOT_CONFIGURED');
   }
 
@@ -93,12 +95,24 @@ async function generateToken(): Promise<string> {
     throw new ApiError(502, 'Could not authenticate with BilalSadaSub', 'BILALSADASUB_AUTH_FAILED');
   }
 
-  cachedToken = body.AccessToken;
-  return cachedToken;
+  cachedGeneratedToken = body.AccessToken;
+  return cachedGeneratedToken;
 }
 
+/**
+ * Static token wins if set (see env.ts). Otherwise falls back to the
+ * cached/auto-generated one from username+password. Neither configured ->
+ * throws, surfaced to the caller as a clear 500 rather than a confusing
+ * downstream failure.
+ */
 async function getToken(): Promise<string> {
-  if (cachedToken) return cachedToken;
+  if (hasStaticToken()) return env.BILALSADASUB_API_TOKEN!;
+  if (cachedGeneratedToken) return cachedGeneratedToken;
+
+  if (!hasCredentials()) {
+    throw new ApiError(500, 'BilalSadaSub is not configured', 'BILALSADASUB_NOT_CONFIGURED');
+  }
+
   // Several purchase requests can land while the very first token
   // generation is still in flight (e.g. a burst of requests right after a
   // cold start) - share one in-flight request instead of firing one
@@ -112,10 +126,11 @@ async function getToken(): Promise<string> {
 }
 
 /**
- * POSTs to any BilalSadaSub endpoint with the cached token, transparently
- * regenerating the token and retrying ONCE if the first attempt comes back
- * 401 (token expired/rotated on their end - "Rotate it from Pricing → API
- * token if it leaks" in the docs implies tokens CAN change server-side).
+ * POSTs to any BilalSadaSub endpoint with the current token, transparently
+ * regenerating the token and retrying ONCE on a 401 - but only when
+ * running on username+password auth. A static token has no credentials to
+ * regenerate from, so a 401 with one configured is logged clearly instead
+ * of being silently retried forever.
  */
 async function authedPost(path: string, body: Record<string, unknown>, retried = false): Promise<BilalResponse> {
   const token = await getToken();
@@ -125,10 +140,13 @@ async function authedPost(path: string, body: Record<string, unknown>, retried =
     body: JSON.stringify(body)
   });
 
-  if (response.status === 401 && !retried) {
-    cachedToken = null;
+  if (response.status === 401 && !retried && !hasStaticToken()) {
+    cachedGeneratedToken = null;
     return authedPost(path, body, true);
   }
+  if (response.status === 401 && hasStaticToken()) {
+    console.error('[bilalsadasub] static BILALSADASUB_API_TOKEN was rejected (401) - it may be revoked or wrong');
+  }
 
   return (await response.json().catch(() => ({}))) as BilalResponse;
 }
